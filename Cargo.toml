[package]
name = "localsetup"
version = "0.1.0"
edition = "2024"

[dependencies]
toml = "0.9.5"
reqwest = {version = "0.12.23", features = ["blocking"]}
serde = {version = "1.0.219", features = ["derive"]}
